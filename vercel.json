{
  "buildCommand": "pnpm install && pnpm exec tsc --build && pnpm --filter @workspace/finbridge run build",
  "outputDirectory": "artifacts/finbridge/dist",
  "framework": null,
  "build": {
    "env": {
      "BASE_PATH": "/"
    }
  },
  "rewrites": [
    { "source": "/(.*)", "destination": "/index.html" }
  ]
}
